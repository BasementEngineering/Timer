# Timer

The timer library allows you to create different timers that can be started, stopped, reset and asked if they are finished. 
It is pretty much a nicely wrapped version of the "millis() - starttime-Concept".
